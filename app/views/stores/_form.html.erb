<h1><%= @store.id ? 'Edit Sub' : 'Create Sub' %></h1>
<hr />
<%= form_for @store do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name, required: true, autofocus: true %>
  <%= f.submit %>
<% end %>